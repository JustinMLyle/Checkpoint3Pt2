@model IEnumerable<ACCUniversity.Models.CampusLocations>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CampusCity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CampusCity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.campusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.campusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.campusId })
        </td>
    </tr>
}

</table>
